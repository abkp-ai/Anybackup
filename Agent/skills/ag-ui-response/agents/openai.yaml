interface:
  display_name: "AG-UI Markdown 回复"
  short_description: "生成标准 AG-UI 事件，并包装到 MQ payload.ag_ui_event 对象字段"
  default_prompt: "Use $ag-ui-response before visible business actions to generate standard AG-UI events, validate them, and publish them as payload.ag_ui_event objects in Decision Agent MQ messages."
